<h1>検索結果</h1>

<p>合計件数: <%= @rooms.count %>件</p>

<% if @rooms.any? %>
  <ul>
    <% @rooms.each do |room| %>
      <li>
        <h2><%= link_to room.name, room_path(room) %></h2>
        <p>住所: <%= room.address %></p>
        <p><%= room.description %></p>
        <p>宿泊料金: <%= room.price %>円/日</p>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>該当する施設が見つかりませんでした。</p>
<% end %>

<p><%= link_to '検索ページに戻る', 'javascript:history.back()', class: 'btn btn-secondary' %></p>


